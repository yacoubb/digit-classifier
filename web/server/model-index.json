{
	"digits": "A convolutional neural net trained to recognise the digits 0-9. Trained on the MNIST dataset.",
	"letters": "A convolutional neural net trained to recognise the letters A-Z. Trained on the EMNIST dataset.",
	"balanced": "A convolutional neural net trained to recognise the numbers 0-9, letters A-Z and some lowercase letters. Trained on the balanced EMNIST dataset."
}
